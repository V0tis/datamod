/** @deprecated 개별 토큰 — 신규는 `@/lib/chartTheme` */
export {
  chartAxisMuted as CHART_GRAY_AXIS,
  chartGridMuted as CHART_GRAY_GRID,
  chartColors,
  chartCardStyle,
  chartCardClassName,
  chartFontFamily,
  formatChartInt,
  mixTealBar,
  divergingFillFromDelta,
} from '@/lib/chartTheme'

/** 레거시 민트 라인·영역 (검색 트렌드 등) */
export const CHART_MINT = '#2AC1BC'
export const CHART_MINT_SOFT = 'rgba(42, 193, 188, 0.25)'
